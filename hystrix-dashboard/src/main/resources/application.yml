eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 10000
spring:
  application:
    name: hystrix-dashboard

#management:
#  endpoints:
#    web:
#      exposure:
#        include: hystrix.stream, info, health
